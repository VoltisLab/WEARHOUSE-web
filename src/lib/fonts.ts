import { Nunito } from "next/font/google";

/** Nunito Black (900) - brand wordmark / logo text. */
export const fontBrandWordmark = Nunito({
  weight: "900",
  subsets: ["latin"],
  display: "swap",
});
